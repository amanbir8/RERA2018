@model CRUD.Models.HelpdeskAdmin.ClsPrp_AdminDesk_NoticeSectionFiveNineByLA_FileLogDetails

@{
    Layout = null;
}

@*<hr />*@
<div class="row">
    <div class="col-md-12">
        <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover" style="width: 100%;">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.EventAction_IdentifiedOn)</th>
                        <th>@Html.DisplayNameFor(model => model.Related_NoticeFile_NumberDetails)</th>
                        <th>@Html.DisplayNameFor(model => model.EventAction_Aggregate)</th>
                        @*<th>@Html.DisplayNameFor(model => model.AssignedTo)</th>*@
                        <th>@Html.DisplayName("Prepared By")</th>
                        <th>@Html.DisplayNameFor(model => model.Remarks_IfAny)</th>                        
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.prpNoticeSectionFiveNineFileLog)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.EventAction_IdentifiedOn)</td>
                            <td>@Html.DisplayFor(modelItem => item.Related_NoticeFile_NumberDetails)</td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EventAction_Aggregate)
                                @Html.DisplayFor(modelItem => item.ProgressStatus)
                            </td>
                            @*<td>@Html.DisplayFor(modelItem => item.AssignedTo)</td>*@
                            @if (item.EventAction_IdentifiedBy != "999")
                            {
                                <td>@Html.DisplayFor(modelItem => item.EventAction_IdentifiedBy)</td>
                            }
                            else
                            {
                                <td>@Html.DisplayName(" ")</td>
                            }
                            <td align="justify">@Html.DisplayFor(modelItem => item.Remarks_IfAny)</td>                            
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@*<hr />
<div class="row">
    <div class="col-md-12">
        <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover" style="width: 100%;">
                <thead>
                    <tr>
                        <th>@Html.DisplayName("Last Updated On")</th>
                        <th>@Html.DisplayName("Application Status")</th>                        
                        <th>@Html.DisplayName("Prepared By")</th>
                        <th>@Html.DisplayName("Remarks If Any")</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.CurrentStatusEventDetails)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.CurrentEvent_IdentifiedOn)</td>                            
                            <td>@Html.DisplayFor(modelItem => item.CurrentEvent_IdentifiedAggregateName)</td>                            
                            @if (item.CurrentEvent_IdentifiedBy != "999")
                            {
                                <td>@Html.DisplayFor(modelItem => item.CurrentEvent_IdentifiedBy)</td>
                            }
                            else
                            {
                                <td>@Html.DisplayName(" ")</td>
                            }
                            <td align="justify">@Html.DisplayFor(modelItem => item.CurrentStatusWithRemarks)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>*@
